name: smart_hotel
description: "StayKey - Smart Hotel Booking & Digital Key System"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # QR code generation
  qr_flutter: ^4.1.0
  # UUID generation for unique booking IDs
  uuid: ^4.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
